import styled from "@emotion/styled";

export const HeaderTapLeft = styled.div`
  position: relative;
  width: ${(props) => props.w + "px"}625px;
  height: ${(props) => props.h + "px"}50px;
  display: flex;
  align-items: center;
  justify-content: space-between;
`;
